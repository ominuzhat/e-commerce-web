/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "live.themewild.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
